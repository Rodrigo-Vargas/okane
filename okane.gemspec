Gem::Specification.new do |s|
  s.name          = "okane"
  s.version       = "0.0.5"
  s.summary       = "A gem to parse OFX files"
  s.authors       = ["Rodrigo Vargas"]
  s.require_paths = ['lib']
  s.files         = `git ls-files -- lib/* `.split("\n")
end
